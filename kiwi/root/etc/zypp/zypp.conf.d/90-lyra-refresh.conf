[main]
# Keep routine CLI operations responsive while preserving automatic refresh.
# Vega performs an explicit refresh before planning and applying updates.
repo.refresh.delay = 2880
download.max_concurrent_connections = 5
download.use_deltarpm = false
